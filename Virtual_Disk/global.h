#include<iostream>


#pragma once
namespace houchenxi
{	

	enum File_Mode
	{
		INVALID_FILE_MODE=-1,
		FILE ,
		SYMLINK,
		SYMLINKD,
		DIROCTRY
	};

	enum ERROR
	{
		FAILURE = -1,
		SUCESS,
		INPUT_ERROR,
		NULL_PTR_ERROR,
		INVALID_OP_ERROR,
		INVALID_PATH_ERROR,
		MUST_DIR_ERROR,
		INVALID_FILE_MODE_ERROR,
		NAME_CONFLICT_ERROR,
		ONLY_DEL_DIR_ERROR,
		ONLY_DEL_EMPTY_DIR_ERROR,
		DST_MUST_DIR_ERROR,
		MK_DIR_ERROR,
		OPEN_DISK_FILE_ERROR,
		
		ILLEGAL_NAME_ERROR,
		NO_DST_FILE_ERROR,
		DST_NAME_EXIST_ERROR,

		MOVE_MUST_DIR_ERROR,

		LINK_NULL_ERROR,

		MUST_DISK_PATH_ERROR,

		MUST_VIR_FILE_ERROR,

		INVALID_CMD_ERROR
	};

	enum COMMAND
	{
		DIR,
		MD,
		RD,
		CD,
		DEL,
		COPY,
		REN,
		MOVE,
		MKLINK,
		SAVE,
		LOAD,
		CLS
	};



};